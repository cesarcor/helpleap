import React from 'react';
import styles from './assigned-user.module.scss';

const AssignedUser = () => {
	return (
		<div className={styles.assigned_user}>
			<span className={styles.assigned_user_name}>A.I.</span>
		</div>
	);
};

export default AssignedUser;
