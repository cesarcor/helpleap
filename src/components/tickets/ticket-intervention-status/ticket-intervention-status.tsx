import React from 'react';
import styles from './ticket-intervention-status.module.scss';

const TicketInterventionStatus = () => {
	return (
		<div className={styles.ticket_intervention_status}>
			<span>Normal</span>
		</div>
	);
};

export default TicketInterventionStatus;
